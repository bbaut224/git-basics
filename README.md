

## 📌 **Инструкция: Как связать локальный репозиторий с удалённым**

### 🔹 Шаг 1. Регистрация на платформе
Выберите одну из популярных платформ:
- **GitHub**: https://github.com  
- **GitLab**: https://gitlab.com  
- **Bitbucket**: https://bitbucket.org  
- **Gitea / Gitee / Codeberg** — альтернативы

1. Перейдите на сайт выбранной платформы.
2. Нажмите **Sign up** (или «Зарегистрироваться»).
3. Укажите:
   - Имя пользователя (username)
   - Email
   - Пароль
4. Подтвердите email (если требуется).
5. Войдите в аккаунт.

---

### 🔹 Шаг 2. Установка Git (если ещё не установлен)

Проверьте, установлен ли Git:

```bash
git --version
```

Если команда не найдена — установите:

- **Windows**: [https://git-scm.com/download/win](https://git-scm.com/download/win)  

---

### 🔹 Шаг 3. Настройка Git (один раз на компьютере)

Укажите своё имя и email — они будут отображаться в истории коммитов:

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

Проверить настройки:

```bash
git config --list
```

> ⚠️ Email должен совпадать с тем, что вы указали при регистрации на GitHub/GitLab, иначе коммиты не будут привязаны к вашему профилю.

---

### 🔹 Шаг 4. Создание нового проекта на платформе

1. Зайдите в свой аккаунт.
2. Нажмите **«New repository»** (или «Создать репозиторий»).
3. Укажите:
   - Название репозитория (например, `my-first-project`)
   - Описание (опционально)
   - Публичный или приватный
   - ❌ **Не ставьте галочку** «Initialize this repository with a README» — если вы уже начали проект локально!
4. Нажмите **Create repository**.

Скопируйте URL репозитория (HTTPS или SSH):
- Пример HTTPS: `https://github.com/ваш-логин/my-first-project.git`
- Пример SSH: `git@github.com:ваш-логин/my-first-project.git`

> 💡 HTTPS проще для новичков. SSH безопаснее и удобнее при частом использовании (но требует настройки ключей).

---

### 🔹 Шаг 5. Инициализация локального репозитория

Если у вас ещё нет локального репозитория:

```bash
mkdir my-first-project
cd my-first-project
git init
```

Если проект уже существует — перейдите в его папку и убедитесь, что там нет `.git`. Если нет — выполните `git init`.

Добавьте файлы в отслеживание:

```bash
git add .
# или конкретный файл: git add README.md
```

Сделайте первый коммит:

```bash
git commit -m "Initial commit"
```

> 💡 Полезные флаги для `git commit`:
> - `-m "сообщение"` — добавить сообщение сразу
> - `--amend` — исправить последний коммит (осторожно!)
> - `-a` — автоматически добавить изменения в отслеживаемых файлах (но не новые файлы)

---

### 🔹 Шаг 6. Связывание с удалённым репозиторием

Выполните команду:

```bash
git remote add origin <URL_вашего_репозитория>
```

Пример:

```bash
git remote add origin https://github.com/ваш-логин/my-first-project.git
```

> 💡 Что делает эта команда?
> - `remote` — управление «удалёнными» репозиториями
> - `add` — добавить новый удалённый репозиторий
> - `origin` — стандартное имя для основного удалённого репозитория (можно дать другое, но `origin` — общепринятое)
> - `<URL>` — адрес вашего репозитория

Проверить, что всё добавилось:

```bash
git remote -v
```

Вывод должен быть примерно таким:

```
origin  https://github.com/ваш-логин/my-first-project.git (fetch)
origin  https://github.com/ваш-логин/my-first-project.git (push)
```

---

### 🔹 Шаг 7. Отправка кода на удалённый репозиторий

Первый push (отправка):

```bash
git push -u origin main
```

> 💡 Почему `main`?
> Современные репозитории используют ветку `main` по умолчанию (раньше было `master`). Узнать текущую ветку можно так:
> ```bash
> git branch
> ```
> Если у вас `master`, используйте `git push -u origin master`.

Флаг `-u` (или `--set-upstream`) связывает локальную ветку с удалённой. После этого можно просто писать:

```bash
git push
git pull
```

без указания `origin main`.

---

### 🔹 Полезные дополнительные команды

| Команда | Назначение |
|--------|-----------|
| `git status` | Показать состояние рабочей директории |
| `git log --oneline` | Краткая история коммитов |
| `git remote show origin` | Подробная информация об удалённом репозитории |
| `git remote rename origin upstream` | Переименовать удалённый репозиторий |
| `git remote remove origin` | Удалить привязку к удалённому репозиторию |
| `git branch -M main` | Переименовать текущую ветку в `main` |

---

готовить PDF-версию или чек-лист для распечатки 😊
